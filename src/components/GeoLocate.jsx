function GeoLocate({ handleClick }) {
    return(
        <button className="geolocate-button mouse-pointer hover:bg-blue-200" onClick={handleClick}>
        </button>
    )
}

export default GeoLocate;